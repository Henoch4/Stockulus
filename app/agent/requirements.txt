# TARS-Solana Python Agent Dependencies
# (versions below are the working set on Python 3.14 / Windows)

# Core
anchorpy==0.21.0
solders==0.26.0
httpx==0.28.1
pyyaml==6.0.3
numpy==2.5.2
python-dotenv==1.2.2

# Optional: HMM regime model (falls back to rule-based if unavailable)
# hmmlearn==0.3.0

# Desk server (web/ + /metrics/* endpoints)
fastapi==0.139.2
uvicorn==0.51.0

# Testing
# pytest==8.0.0
# pytest-asyncio==0.23.0